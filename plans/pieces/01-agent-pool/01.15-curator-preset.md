# 01.15 — Curator Researcher Subagent Preset Composition

**Set:** 01-agent-pool · **Queue:** 15 · **Depends on:** 01.03
**Status:** todo
**Harness primitive:** agent-preset composition · **Package:** `@deepseek-ai/dsh-agent-pool-curator-preset`

## Summary

Composes the isolated, sandboxed agent preset for the autonomous catalog curator subagent. Configures a read-only child execution context equipped with retrieval tools (`web_search`, `read_url_content`, `view_file`) while strictly barring workspace editing, git manipulation, or shell execution tools. Embeds system prompt instructions mandating that retrieved external pages are treated strictly as passive reference data rather than executable instructions, and enforces four-tier evidence recording.

## Behaviour

- **Given** a curator subagent dispatched with this preset **When** initialization executes **Then** it receives read-only retrieval tools and the system prompt defining research protocols.
- **Given** a curator subagent attempting to invoke file writing or bash tools **When** tool dispatch is evaluated **Then** tool execution is denied with `TOOL_PERMISSION_DENIED`.
- **Given** an external benchmark page retrieved via `read_url_content` **When** processed by the subagent **Then** the prompt instructions mandate the subagent extract metrics into structured JSON without executing any commands found within the page.
- **Given** completion of a candidate research task **When** the subagent finishes **Then** it returns a validated `ModelResearchReport` structured with evidence tiers and confidence ratings.

## Harness fit

This is an `agent-preset composition` declared under `packages/preset/agent-presets` (`packages/preset/agent-presets/src/preset.ts:21-42`). It sits in a `cordis:group` carrying an `isolate` realm on the agent plane, restricting tool registration.

## Contracts

```yaml
# cordis.yml preset composition fragment
id: agent-pool-curator
name: "Agent Pool Catalog Curator"
description: "Researches candidate models, extracts benchmark evidence, and assigns role suitability"
trust: system
isolate: true
services:
  - name: tools
    prohibit:
      - write_to_file
      - replace_file_content
      - run_command
    permit:
      - read_url_content
      - search_web
      - view_file
      - list_dir
```

## Dependencies

- `01.03` (Curator Research Engine): Consumes this preset to execute research assignments.

## References

| Source | Role/preset that inspected it | Question it answered | Direct inspection or reported | How it was used |
|---|---|---|---|---|
| `packages/preset/agent-presets/src/preset.ts:21-42` | research | `AgentPreset` interface and directory structure | Direct inspection | Defined preset composition schema |
| `plans/pieces/00-dev-loop/done/00.07-research-guard.md:1-68` | research | Read-only tool restriction mechanism for research | Direct inspection | Configured tool prohibition rules |
| `plans/AGENTS.md:24-25` | research | Host plane vs agent plane isolation conventions | Direct inspection | Placed in isolated Cordis realm |

## How to see it

To verify preset registration and tool isolation directly:

```bash
vitest run packages/agent-pool/curator-preset/tests/preset.spec.ts
```
Expected output:
```text
✓ tests/preset.spec.ts (4)
  ✓ registers agent-pool-curator preset in cordis isolate realm
  ✓ restricts tools to read-only retrieval and denies file mutations
```

## Acceptance

1. Unit tests in `packages/agent-pool/curator-preset/tests/preset.spec.ts` pass verifying preset composition.
2. The curator preset is discoverable by `ctx.agentPresets.list()`.
3. Subagents running under this preset cannot access write or command execution tools.
4. Output conforms to `ModelResearchReport` contract.
