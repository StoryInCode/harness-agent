# 00.09a — BDD Scenario Heading and Bullet AST Parser

**Set:** 00-dev-loop · **Queue:** 26 · **Depends on:** 00.01e, 00.08c
**Lead Developer:** 🐾 Neko-chan (Inspector Cat)
**Status:** todo
**Harness primitive:** Service Definition · **Package:** `@deepseek-ai/dsh-dev-loop-test-handoff`

## Summary

Nya~ (=^･ω･^=)🐾 Let's find the game rules! Every piece has a `## Behaviour` section with shiny Given/When/Then bullet points! Inspector Cat parses those bullets into structured test scenarios so our Test Writer can author failing tests before anyone touches code. TDD is the happiest way to build software!

### Human Decision Point: Option A vs Option B
- **Option A (Active)**: Extract Given/When/Then scenario triplets using explicit bullet marker parsing.
- **Option B (Alternative)**: Allow freeform prose without Given/When/Then markers, making behavioral testing ambiguous.

## Behaviour

- **Given** a `## Behaviour` section with 3 valid Given/When/Then bullets **When** extractScenarios is called **Then** it returns 3 PieceScenario objects with given, when, and then fields.
- **Given** a bullet that omits the **Then** clause **When** extractScenarios parses the section **Then** it reports a malformed scenario warning.
- **Given** a piece with zero behavioral scenarios **When** extractScenarios runs **Then** it reports a blocker finding requiring at least one BDD scenario.

## Harness fit

Pure parser in `@deepseek-ai/dsh-dev-loop-test-handoff/src/extract.ts`. Feeds test scenarios to `ctx.devLoopTestHandoff`.

## Contracts

```typescript
export interface PieceScenario {
  readonly given: string
  readonly when: string
  readonly then: string
}

export function extractScenarios(content: string): readonly PieceScenario[]
```

## Dependencies

- **Prerequisites:** 00.01e, 00.08c

## References

| Source | Role/preset that inspected it | Question it answered | Direct inspection or reported | How it was used |
|---|---|---|---|---|
| `packages/dev-loop/directory/src/parse.ts:332-347` | 🐾 Neko-chan (Inspector Cat) | How are Given/When/Then markers parsed from `## Behaviour` bullet lines? | direct inspection | Verified `readScenario` marker extraction logic and slicing |
| `packages/dev-loop/directory/src/types.ts:44-52` | 🐾 Neko-chan (Inspector Cat) | What is the structured contract for extracted `PieceScenario` objects? | direct inspection | Aligned `PieceScenario` interface with existing directory types |
| `packages/dev-loop/directory/tests/directory.spec.ts:654-712` | 🐾 Neko-chan (Inspector Cat) | How does the existing test suite verify BDD scenario parsing edge cases? | direct inspection | Examined test cases for incomplete markers and missing sections |
| `plans/pieces/PIECE-FORMAT.md:58-62` | 🐾 Neko-chan (Inspector Cat) | What is the canonical format for BDD Given/When/Then scenarios in micro-gate specs? | direct inspection | Confirmed format requirements for scenario bullet points |

## How to see it

Run:
```bash
pnpm exec vitest run packages/dev-loop/test-handoff/tests/extract.spec.ts
```
Expected output:
```
✓ extractScenarios > extracts Given/When/Then triplets cleanly
```

## Teach me while you build

How do we know what a program should do? We write the rules in plain English first! Nya~

- **What it does**: Parses BDD scenarios from markdown so automated test runners can generate test stubs.
- **Background**: Behavior-Driven Development (BDD) uses Given (precondition), When (action), and Then (expected result) to define acceptance criteria.
- **How it works here**: Scans lines in `## Behaviour`. Looks for `- **Given** `, locates ` **When** ` and ` **Then** `, and extracts the text between them.
- **Why this approach**: Enforces clear behavioral thinking without requiring complex Cucumber Gherkin parsers.
- **Approaches considered**: Full Gherkin parser (heavy, outside dependency) vs Freeform text (unstructured) vs Markdown BDD triplets (chosen: clean and native).
- **Prior art inspected**: Cucumber, Dan North's BDD specification, and Vitest test scenario runners.
- **What we get for free**: Standard markdown list formatting renders beautifully on GitHub and in terminals.
- **From the docs**: plans/AGENTS.md: 'Every piece carries: Summary, Behaviour (Given/When/Then), Harness fit...'
- **What to notice**: Notice how each clause is trimmed so whitespace around bold markers is cleaned up!
- **Concepts to own**: Specification by example: using concrete scenarios to communicate requirements unambiguously.
- **Interview angle**: How do you bridge the gap between product requirements and automated unit tests? Answer: BDD Given/When/Then scenarios parsed into test suites.
- **Decisions you can now make alone**: You can add support for `And` continuation clauses in scenarios.
- **What you should learn from this**: Define the behavior before writing the code, and bugs will have nowhere to hide! Purr~

## Resources and proof

| Claim | Citation | How established | Checked against |
|---|---|---|---|
| Scenario parser extracts Given/When/Then triplets from bullet lines using marker boundaries | `packages/dev-loop/directory/src/parse.ts:332-347` | direct inspection | Current checkout |
| PieceScenario contract defines readonly given, when, and then text fields | `packages/dev-loop/directory/src/types.ts:44-52` | direct inspection | Current checkout |
| Unit tests verify scenario triplet extraction and filtering of non-scenario lines | `packages/dev-loop/directory/tests/directory.spec.ts:654-712` | direct inspection | Current checkout |

**Learning resources**

- Martin Fowler on GivenWhenThen BDD specification standard — https://martinfowler.com/bliki/GivenWhenThen.html
- Reference implementation of scenario bullet parsing — `packages/dev-loop/directory/src/parse.ts:332-347`
- Test suite verifying scenario parsing edge cases and non-scenario line filtering — `packages/dev-loop/directory/tests/directory.spec.ts:654-712`
- Canonical micro-gate format specification for Behaviour sections — `plans/pieces/PIECE-FORMAT.md:58-62`

## Reuse capture

Reuse candidate: Markdown BDD Triplet Extractor
What it does: Parses Given/When/Then scenarios from markdown lists.
Current usage: Used in `dsh-dev-loop-test-handoff`.
Potential reuse: Can generate automated test scaffolding from user stories.
What would need to become generic: Pure string parsing function.
Evidence: packages/dev-loop/test-handoff/src/extract.ts
Confidence: high

## Acceptance

- [ ] Extracts Given, When, Then clauses.
- [ ] Enforces at least one scenario per piece.
- [ ] Returns structured scenario array.
- Next Gate: [`00.09b`] — Proceed to next verified milestone.
