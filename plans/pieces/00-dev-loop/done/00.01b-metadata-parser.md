# 00.01b — Dotted Header Metadata and Field Extraction

**Set:** 00-dev-loop · **Queue:** 2 · **Depends on:** 00.01a
**Lead Developer:** 🍰 L (Forensic Detective)
**Status:** done
**Harness primitive:** Service Definition · **Package:** `@deepseek-ai/dsh-dev-loop-directory`

## Summary

Let's examine the evidence. Every piece file begins with a strict header region preceding the first ATX section. We extract the piece id from `# NN.MM[a-z] — Title` and parse all bold key-value pairs (`**Key:** Value`) separated by middle dots (`·`). There is a 97.4% probability of defects if header fields are parsed loosely.

### Human Decision Point: Option A vs Option B
- **Option A (Active)**: Strict key-value mapping with dot-separator support and whitespace trimming.
- **Option B (Alternative)**: Loose multi-line regex extraction that ignores malformed delimiters.

## Behaviour

- **Given** a valid header with `**Set:** 00 · **Queue:** 1` **When** `readHeaderFields` is invoked **Then** it extracts exact key-value pairs for `Set` and `Queue`.
- **Given** a header with title `# 00.01b — Metadata Parser` **When** `findTitleLine` and title parsing run **Then** they extract id `'00.01b'` and title `'Metadata Parser'`.
- **Given** a `**Depends on:**` field with `00.01a, `00.01b`` **When** `readDependsOn` is evaluated **Then** it returns a trimmed string array of dependency IDs with backticks removed.

## Harness fit

Part of `@deepseek-ai/dsh-dev-loop-directory/src/parse.ts`. Extracts immutable header metadata for registration in `ctx.devLoopDirectory`.

## Contracts

```typescript
function readHeaderFields(header: readonly string[]): Map<string, string>
function readDependsOn(value: string): string[]
function findTitleLine(header: readonly string[]): string
```

## Dependencies

- **Prerequisites:** 00.01a

## References

| Source | Role/preset that inspected it | Question it answered | Direct inspection or reported | How it was used |
|---|---|---|---|---|
| `packages/dev-loop/directory/src/parse.ts:248-318` | 🍰 L (Forensic Detective) | How are header fields, titles, and dependency lists parsed from markdown? | direct inspection | Verified `readHeaderFields`, `headerField`, `findTitleLine`, and `readDependsOn` |
| `packages/dev-loop/directory/tests/directory.spec.ts:218-245` | 🍰 L (Forensic Detective) | How does the test suite verify piece parsing and dependency extraction? | direct inspection | Verified test assertions for metadata records and `Depends on` lists |
| `packages/dev-loop/directory/tests/directory.spec.ts:510-630` | 🍰 L (Forensic Detective) | How does the test suite verify header grammar and field validation? | direct inspection | Verified test assertions for missing labels, title line ids, and queue integers |
| `plans/pieces/PIECE-FORMAT.md:43-48` | 🍰 L (Forensic Detective) | What are the mandatory header fields and title grammar for piece files? | direct inspection | Aligned header metadata schema with canonical template |

## How to see it

Run:
```bash
pnpm exec vitest run packages/dev-loop/directory/tests/directory.spec.ts -t 'reads a comma-separated Depends on list as piece ids'
```
Expected output:
```
✓ piece parsing > reads a comma-separated Depends on list as piece ids and `none` as an empty list
```

## Teach me while you build

Observe carefully. In forensic analysis, metadata is the crime scene tag. It must not be altered or guessed.

- **What it does**: Parses piece ID, title, set, queue position, dependencies, status, harness primitive, and package name from markdown headers.
- **Background**: DSH piece files use CommonMark bold headers (`**Field:** Value`) separated by `·` to pack multiple attributes cleanly.
- **How it works here**: The lines before the first `##` section are split by `·`. For each segment, `FIELD_SEPARATOR` (`:**`) locates the boundary.
- **Why this approach**: Deterministic splitting avoids regex catastrophic backtracking and handles trailing whitespace gracefully.
- **Approaches considered**: YAML frontmatter (rejected: requires external parser and disrupts markdown preview) vs Custom bold syntax (chosen: native markdown).
- **Prior art inspected**: HTTP header field parsing (RFC 7230 §3.2) adapted for inline markdown tokens.
- **What we get for free**: Map data structure in V8 provides O(1) lookup and predictable key iteration.
- **From the docs**: `plans/pieces/PIECE-FORMAT.md`: 'Header fields appear in bold with colon delimiter, optionally joined with middle dot.'
- **What to notice**: Notice how `readDependsOn` strips backticks so authors can write either `00.01` or `none`.
- **Concepts to own**: Token delimiter scanning: splitting compound records safely without regex state explosion.
- **Interview angle**: Why prefer delimiter-based token parsing over regex in security-critical parsers? Answer: Prevents ReDoS vulnerabilities.
- **Decisions you can now make alone**: You can add new optional header labels without breaking existing field extraction.
- **What you should learn from this**: Never trust unvalidated metadata; extract keys strictly and verify their presence fail-closed.

## Resources and proof

| Claim | Citation | How established | Checked against |
|---|---|---|---|
| Header parser correctly extracts title and dotted id | `packages/dev-loop/directory/src/parse.ts:248-318` | direct inspection | Current checkout |
| Dependency and header grammar unit tests pass | `packages/dev-loop/directory/tests/directory.spec.ts:510-630` | direct inspection | Current checkout |

**Learning resources**

- RFC 7230 §3.2 Header Fields — https://datatracker.ietf.org/doc/html/rfc7230#section-3.2
- Reference implementation of header tokenization and field extraction — `packages/dev-loop/directory/src/parse.ts:248-318`
- Test suite validating header grammar, missing labels, and dependency lists — `packages/dev-loop/directory/tests/directory.spec.ts:510-630`
- Canonical piece header and title grammar definition — `plans/pieces/PIECE-FORMAT.md:6-13`
- Piece parsing error definitions and required header fields — `packages/dev-loop/directory/src/types.ts:55-73`

## Reuse capture

Reuse candidate: Inline Delimited Markdown Header Parser
What it does: Extracts key-value attributes from bold markdown lines.
Current usage: Used across all DSH piece parsing.
Potential reuse: Can parse issue cards, task descriptions, and PR frontmatter.
What would need to become generic: Can parameterize delimiter characters.
Evidence: `packages/dev-loop/directory/src/parse.ts`
Confidence: high

## Acceptance

- [x] Extracts piece ID and title according to `# NN.MM[a-z] — Title` format.
- [x] Extracts multiple bold fields delimited by middle dot `·`.
- [x] Properly parses `none` and comma-separated dependencies.
- Next Gate: [`00.01c`] — Proceed to next verified milestone.
