# 00.06a — Canonical Dev Loop Role Definitions

**Set:** 00-dev-loop · **Queue:** 19 · **Depends on:** 00.05c
**Lead Developer:** 🌸 Mayuri (Gentle Seamstress)
**Status:** done
**Harness primitive:** Service Definition · **Package:** `@deepseek-ai/dsh-dev-loop-roles`

## Summary

Tutturu~ 🌸 Every member of our lab has a special talent! In the Dev Loop, we have 4 canonical specialist roles: Research (to explore facts), Test Writer (to author failing red tests), Implementer (to write the code that makes tests green), and Utility (for cleanups)! Giving each worker a clear role makes everyone happy and prevents anyone from stepping on each other's toes!

### Human Decision Point: Option A vs Option B
- **Option A (Active)**: Enforce 4 canonical specialized roles with explicit, dedicated accountability boundaries.
- **Option B (Alternative)**: Use a single generic 'coder' role that writes tests and code simultaneously, leading to untested code.

## Behaviour

- **Given** a delegation request specifying canonical role 'Test Writer' **When** `briefSchema.parse` is called **Then** it validates successfully and retains the brief for child dispatch.
- **Given** a delegation request specifying canonical role 'Implementer' **When** `briefSchema.parse` is called **Then** it validates successfully and binds the assignment to the worktree.
- **Given** a request with an unknown role name 'Architect' or 'Reviewer' **When** `briefSchema.parse` runs **Then** it rejects fail-closed with schema validation error.

## Harness fit

Role type definitions in `packages/dev-loop/roles/src/types.ts` and schema validation in `packages/dev-loop/roles/src/records.ts`. Configures role accounts for `ctx.devLoopRoles`.

## Contracts

```typescript
export type DevLoopRole = 'Research' | 'Test Writer' | 'Implementer' | 'Utility'

export const roleNames = ['Research', 'Test Writer', 'Implementer', 'Utility'] as const

export interface RoleConfig {
  readonly provider: string
  readonly persona: string
  readonly toolFilter: {
    readonly allow?: readonly string[]
    readonly deny?: readonly string[]
  }
  readonly agentOptions?: Required<AgentOptions>
  readonly maxDepth?: number
}
```

## Dependencies

- **Prerequisites:** 00.05c

## References

| Source | Role/preset that inspected it | Question it answered | Direct inspection or reported | How it was used |
|---|---|---|---|---|
| `packages/dev-loop/roles/src/types.ts:10-50` | 🌸 Mayuri (Gentle Seamstress) | How are canonical role types and role configurations defined? | direct inspection | Verified `DevLoopRole` union and `RoleConfig` interface contracts |
| `packages/dev-loop/roles/src/records.ts:10-27` | 🌸 Mayuri (Gentle Seamstress) | How does the brief schema validate role enumeration? | direct inspection | Verified `roleNames` tuple and `briefSchema` Zod validation |
| `packages/dev-loop/roles/src/personas.ts:9-142` | 🌸 Mayuri (Gentle Seamstress) | Where are developer personas and system prompts cataloged? | direct inspection | Verified persona definitions and `getDevLoopPersona` helper |
| `packages/dev-loop/roles/tests/roles.spec.ts:10-42` | 🌸 Mayuri (Gentle Seamstress) | How are all canonical roles tested across child dispatch? | direct inspection | Verified `it.each(roles)` test case covering child recording and cleanup |

## How to see it

Run:
```bash
pnpm exec vitest run packages/dev-loop/roles/tests/roles.spec.ts -t 'records the actual child'
```
Expected output:
```
✓ Research records the actual child, caller, assigned cwd and reported provenance after cleanup
```

## Teach me while you build

Just like a stage play, every actor needs their own costume and script! Tutturu~

- **What it does**: Defines the 4 specialized roles used by the Dev Loop orchestrator.
- **Background**: Splitting software development into discrete roles (research, test authoring, implementation) guarantees rigorous TDD.
- **How it works here**: The role union type restricts delegation requests. Each role maps to a tailored system prompt and tool filter.
- **Why this approach**: Prevents implementers from altering tests to fit broken code by separating the Test Writer from the Implementer.
- **Approaches considered**: Single omnipotent agent (fails: writes trivial passing tests) vs 4 specialized roles (chosen: enforces verification gates).
- **Prior art inspected**: Extreme Programming (XP) pair programming and clean-room software engineering.
- **What we get for free**: TypeScript's closed union prevents spelling errors in role briefs at compile time.
- **From the docs**: AGENTS.md: 'A capability seam comprises Service Definition / Service Provider / Consumer roles.'
- **What to notice**: Notice how each role pairs with our developer personas (Neko-chan, L, Daru, Hououin Kyouma, Mayuri)!
- **Concepts to own**: Separation of concerns: assigning distinct responsibilities to autonomous agents to prevent self-serving bias.
- **Interview angle**: Why separate test writing from code implementation in multi-agent workflows? Answer: Prevents the implementer from weakening assertions to claim success.
- **Decisions you can now make alone**: You can customize the persona prompt text in `cordis.yml` for any role without breaking role routing.
- **What you should learn from this**: Clear roles bring harmony and trust to autonomous teams! 🌸

## Resources and proof

| Claim | Citation | How established | Checked against |
|---|---|---|---|
| Roles definition strictly defines 4 canonical roles | `packages/dev-loop/roles/src/types.ts:10` | direct inspection | Current checkout |
| Role names array enforces closed enumeration for brief validation | `packages/dev-loop/roles/src/records.ts:11-27` | direct inspection | Current checkout |
| Persona registry exports reusable personas including Mayuri | `packages/dev-loop/roles/src/personas.ts:9-142` | direct inspection | Current checkout |
| Role dispatch test verifies child execution for each canonical role | `packages/dev-loop/roles/tests/roles.spec.ts:10-42` | direct inspection | Current checkout |

**Learning resources**

- Canonical role types and configuration contracts — `packages/dev-loop/roles/src/types.ts:10-50`
- Zod brief parser and closed role enumeration — `packages/dev-loop/roles/src/records.ts:10-27`
- Five developer persona definitions and system prompt templates — `packages/dev-loop/roles/src/personas.ts:9-142`
- Role delegation test suite executing across all canonical roles — `packages/dev-loop/roles/tests/roles.spec.ts:10-42`
- Canonical micro-gate specification and persona guidelines — `plans/pieces/PIECE-FORMAT.md`

## Reuse capture

Reuse candidate: Canonical Dev Loop Role Schema
What it does: Role definitions and configuration contracts.
Current usage: Used by `dsh-dev-loop-roles`.
Potential reuse: Can guide any multi-agent software engineering framework.
What would need to become generic: Can extend role union if needed.
Evidence: packages/dev-loop/roles/src/types.ts
Confidence: high

## Acceptance

- [x] Defines Research, Test Writer, Implementer, Utility.
- [x] Binds role configurations to Cordis context.
- [x] Exports reusable personas.
- Next Gate: [`00.06b`] — Proceed to next verified milestone.

