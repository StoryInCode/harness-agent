# 00.01c — Line Ceiling and Primitive Enforcement

**Set:** 00-dev-loop · **Queue:** 3 · **Depends on:** 00.01a, 00.01b
**Lead Developer:** 💻 Daru (Super Hacker)
**Status:** done
**Harness primitive:** Service Definition · **Package:** `@deepseek-ai/dsh-dev-loop-directory`

## Summary

Yo! Daru here. Script kiddies love writing 2,000-line monolithic spec files that crash everyone's context window. Not on my watch! We enforce the strict 280-line ceiling and validate the closed vocabulary of Harness primitives. If someone tries to pass 'Plugin' as a primitive, our bouncer kicks them out instantly. LOL.

### Human Decision Point: Option A vs Option B
- **Option A (Active)**: Fail-closed validation throwing `PieceParseError` with structured blocker and warning findings.
- **Option B (Alternative)**: Silent warning logging that allows oversized or malformed pieces to slip into the queue.

## Behaviour

- **Given** a piece file exceeding 280 lines **When** `parsePiece` is executed **Then** it throws `PieceParseError` with blocker code `PIECE_SIZE_EXCEEDED`.
- **Given** a piece declaring 'Plugin' as Harness primitive **When** `parsePiece` checks primitive **Then** it throws `PieceParseError` with blocker code `INVALID_HARNESS_PRIMITIVE`.
- **Given** a piece with all 11 canonical sections in exact order **When** `parsePiece` is called **Then** it passes without blocker findings and returns a valid `PieceRecord`.

## Harness fit

Validation engine inside `@deepseek-ai/dsh-dev-loop-directory/src/parse.ts`. Implements deterministic axiom checking for CI scripts and runtime directory services.

## Contracts

```typescript
export interface PieceFinding {
  readonly code: PieceFindingCode
  readonly severity: 'blocker' | 'warning'
  readonly message: string
}

export class PieceParseError extends Error {
  readonly code: PieceFindingCode
  readonly path: string
  readonly findings: readonly PieceFinding[]
}

export function parsePiece(path: string, content: string, options?: PieceParseOptions): PieceRecord
```

## Dependencies

- **Prerequisites:** 00.01a, 00.01b

## References

| Source | Role/preset that inspected it | Question it answered | Direct inspection or reported | How it was used |
|---|---|---|---|---|
| `packages/dev-loop/directory/src/parse.ts:442-583` | 💻 Daru (Super Hacker) | How does `validatePiece` accumulate findings and `parsePiece` throw `PieceParseError`? | direct inspection | Verified line count checks, primitive validation, and error constructor |
| `packages/dev-loop/directory/tests/directory.spec.ts:255-290` | 💻 Daru (Super Hacker) | How does the test suite verify the 280-line ceiling and line terminator handling? | direct inspection | Verified unit test cases for default ceiling and maxLines overrides |
| `packages/dev-loop/directory/tests/directory.spec.ts:486-508` | 💻 Daru (Super Hacker) | How does the test suite verify closed Harness primitive vocabularies? | direct inspection | Verified unit test assertions for unapproved primitives |
| `plans/AGENTS.md:20-76` | 💻 Daru (Super Hacker) | What are the mandatory rules and primitives defined for harness packages? | direct inspection | Confirmed `R-harness-primitive-declared` and `R-piece-size` axioms |

## How to see it

Run:
```bash
pnpm exec vitest run packages/dev-loop/directory/tests/directory.spec.ts -t 'rejects a file one line over the default ceiling with PIECE_SIZE_EXCEEDED'
```
Expected output:
```
✓ piece size ceiling > rejects a file one line over the default ceiling with PIECE_SIZE_EXCEEDED
```

## Teach me while you build

Listen up! If you don't set boundaries on code or specs, they expand until your memory blows up.

- **What it does**: Validates that a piece file is at most 280 lines, has all required headers, uses canonical primitives, and orders sections correctly.
- **Background**: The R-piece-size axiom limits piece files to 280 lines so agents and humans can digest them in under 2 minutes.
- **How it works here**: `validatePiece` checks line length, validates header fields against `REQUIRED_HEADER_FIELDS`, checks primitive against `HARNESS_PRIMITIVES`, and checks canonical section sequence.
- **Why this approach**: Aggregating all findings into a list before throwing gives the user complete feedback in one go rather than one error at a time.
- **Approaches considered**: Throw-on-first-error (annoying: fix one error, run, get next error) vs Collected findings error (chosen: lists everything).
- **Prior art inspected**: GCC and Rust compiler error diagnostic collectors.
- **What we get for free**: TypeScript discriminated union on `PieceFindingCode` makes error handling exhaustive.
- **From the docs**: `plans/AGENTS.md`: 'A piece file is at most 280 lines... Every piece file declares a Harness primitive from the closed vocabulary.'
- **What to notice**: Notice that warnings don't block parsing unless there is at least one blocker finding!
- **Concepts to own**: Validator pattern: decoupling linting/validation rules from AST data construction.
- **Interview angle**: How do you design a compiler error reporter? Answer: Collect warnings and blockers separately, throw on first blocker threshold.
- **Decisions you can now make alone**: You can add new warnings (e.g. word count suggestions) without blocking valid piece files.
- **What you should learn from this**: Strict limits prevent cognitive overload and maintain agent execution speed! Keep it lean, hacker style.

## Resources and proof

| Claim | Citation | How established | Checked against |
|---|---|---|---|
| Ceiling check rejects oversized files deterministically | `packages/dev-loop/directory/src/parse.ts:450-456` | direct inspection | Current checkout |
| Primitive validator checks against closed vocabulary | `packages/dev-loop/directory/src/parse.ts:508-514` | direct inspection | Current checkout |

**Learning resources**

- Harness mandatory patterns and axioms specification — `plans/AGENTS.md:20-76`
- Reference implementation of line count validation and primitive checking — `packages/dev-loop/directory/src/parse.ts:442-583`
- Test suite verifying line ceilings and primitive vocabulary enforcement — `packages/dev-loop/directory/tests/directory.spec.ts:255-290`
- Canonical micro-gate specification and size rules — `plans/pieces/PIECE-FORMAT.md:4-5`
- Closed vocabulary definition for approved Harness primitives — `packages/dev-loop/directory/src/parse.ts:23-43`

## Reuse capture

Reuse candidate: Diagnostic Finding Accumulator
What it does: Accumulates blocker and warning findings during document validation.
Current usage: Used across all DSH piece and config checkers.
Potential reuse: Reusable for any linter or schema validator in Harness.
What would need to become generic: Severity and code enum can be parameterized.
Evidence: `packages/dev-loop/directory/src/parse.ts`
Confidence: high

## Acceptance

- [x] Rejects files over 280 lines with blocker code.
- [x] Rejects unknown Harness primitives fail-closed.
- [x] Throws `PieceParseError` containing all collected findings.
- Next Gate: [`00.01d`] — Proceed to next verified milestone.
