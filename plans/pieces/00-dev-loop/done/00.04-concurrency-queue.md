# 00.04 — Dev Loop Dispatch Queue and Concurrency Guard

**Set:** 00-dev-loop · **Queue:** 4 · **Depends on:** 00.01, 00.02, 00.03
**Status:** done
**Harness primitive:** Service Provider · **Package:** `@deepseek-ai/dsh-dev-loop-queue`

## Summary

This queue limits how many one-shot delegations run at the same time for approved pieces. Think of it as a parking lot with a fixed number of spaces. A role consumer submits a piece id, a start callback, and a cancellation signal. The queue records who is asking right now, reads dependencies and queue order from the directory, and reserves a space before it calls the callback. It never invents a parent, provider, assignment, role, or worktree from an approval event.

`maxConcurrency` is a validated integer Config field. It accepts 1 through 32 and defaults to 4. This limit belongs to this loop, not to the whole process or to a provider's internal subagents. The default and bounds come from Plan 15 conventions, not from measured machine capacity. A deployment can set 6 without changing code.

## Behaviour

- **Given** a pending piece, a live initiator, and spare capacity **When** its request is admitted **Then** capacity is reserved before its start callback runs.
- **Given** reservations at the configured ceiling **When** another request arrives **Then** its callback waits until a reservation is released.
- **Given** several eligible requests **When** capacity opens **Then** lower canonical queue numbers run first, and FIFO admission order breaks ties.
- **Given** an incomplete dependency **When** scheduling runs **Then** the dependent parks, and a later completion notification wakes it.
- **Given** a blocked dependency **When** scheduling runs **Then** the dependent moves from pending to blocked, its request rejects, and unrelated eligible requests continue.
- **Given** an approval notification without a submitted request **When** the listener runs **Then** no callback or subagent is created.
- **Given** absent or stale initiator identity **When** admission or dispatch checks run **Then** the request rejects without starting a child.
- **Given** a start callback that rejects **When** it settles **Then** its reservation is released without waiting for a subagent event that might never come.
- **Given** a returned run whose result settles **When** cleanup remains pending **Then** the reservation stays occupied until `dispose()` and result observation settle.
- **Given** queued cancellation **When** its signal aborts **Then** no callback runs and the request settles cancelled.
- **Given** starting or running cancellation **When** its signal aborts **Then** the queue waits for startup to settle, disposes any returned run, and joins it before releasing capacity.
- **Given** disposal of the queue **When** teardown begins **Then** admission and wakeups stop, every operation is aborted, and teardown waits for startup and run cleanup.
- **Given** run cleanup failure **When** quiescence cannot be established **Then** the queue closes admission, cancels remaining requests, and reports the failure instead of claiming free capacity.

## Harness fit

A host Service Provider exposes `ctx.devLoopQueue`. Hard dependencies are `agents`, `devLoopDirectory`, and `devLoopLifecycle`. The subagent package supplies the existing run/result types, not a new dispatch method. The role consumer (00.06) builds the real `SubagentStartRequest` and calls `ctx.subagents.start(provider, request)` inside the supplied callback.

The queue captures `ctx.agents.currentInitiator()` before its first await. It checks `ctx.agents.get(agent.id) === agent`, and it checks again before invoking a callback under `ctx.agents.withInitiator(agent, ...)`. Scheduling itself runs without an inherited arbitrary initiator. Live Agent objects, callbacks, and run handles stay private. Status queries return owned scalar records.

The returned one-shot `SubagentRun` already has `id`, `result`, and `dispose()`. Its result and cleanup promises own settlement. So the queue does not guess identity or reclaim capacity by counting global `subagent/start` or `subagent/end` announcements. `id` is a child SessionId, not a run epoch, and this package does not support continuable residency epochs. An unrelated event cannot release a reservation.

`piece/approved`, `piece/completed`, and `piece/blocked` only wake existing requests. They carry no dispatch context. The queue reads dependencies through Directory and authoritative logical status through Lifecycle. A disk todo header is not a replacement for pending memory.

## Contracts

```typescript
import type { Agent } from '@deepseek-ai/dsh-agent'
import type { SessionId } from '@deepseek-ai/dsh-session'
import type { SubagentRun, SubagentResult } from '@deepseek-ai/dsh-subagent'

export interface Config {
  maxConcurrency?: number // integer 1–32, default 4
}

export interface QueueRequest {
  pieceId: string
  signal: AbortSignal
  dispatch(agent: Agent, signal: AbortSignal): Promise<SubagentRun>
}

export interface QueueTicket {
  readonly pieceId: string
  readonly result: Promise<SubagentResult>
  cancel(reason?: unknown): Promise<void> // idempotent; awaits owned cleanup
}

export interface QueueEntry {
  pieceId: string
  queueOrder: number
  enqueuedAt: number
  status: 'queued' | 'starting' | 'running'
  subagentId?: SessionId // available only after the one-shot start returns
}

export interface DevLoopQueue {
  readonly maxConcurrency: number
  enqueue(request: QueueRequest): Promise<QueueTicket>
  getActiveWorkers(): QueueEntry[] // includes starting reservations
  getQueuedEntries(): QueueEntry[]
}
```

Admission validates the piece and its dependencies. It requires lifecycle pending status and rejects a duplicate outstanding piece id. It returns a ticket without waiting for capacity or worker completion. Cancellation rejects the ticket result. `cancel()` resolves only after cleanup, or rejects with its cleanup failure. A settled request allows a later role request for the same still-pending piece. Public records contain no live Agent or run references.

There is no separate dispatcher registry and no public `dequeue()`. The current consumer already owns a per-invocation callback and signal, so another registration lifetime would duplicate that ownership. A role fiber aborts its invocation signal when it unloads. A late callback return after cancellation is joined and disposed, and never admitted as fresh work.

## Dependencies

- `00.01`: canonical piece metadata, dependency ids, and queue order.
- `00.02`: pending admission, dependency status, blocked transition, and wakeup announcements.
- `00.03`: the human path to pending. Its approval result is not itself a dispatch request.
- `00.06` consumes this scheduler. The queue does not inject the role service, so no dependency cycle forms.

## References

| Source | Role/preset that inspected it | Question it answered | Direct inspection or reported | How it was used |
|---|---|---|---|---|
| `packages/subagent/subagent/src/index.ts:545-586`, `types.ts:145-171,308-333` | Research `2c71989f-930e-4292-b974-9d67787c5ab3`, current harness preset | What actually starts a one-shot child and joins it? | Reported by Research | Callback returns the existing `SubagentRun`, not an invented dispatcher API |
| `packages/core/agent/src/index.ts`, `currentInitiator`, `withInitiator`, `withoutInitiator`, `get` | Same Research | How is the calling Agent captured and checked? | Reported by Research | Capture live identity at admission and restore it at dispatch |
| `packages/dev-loop/lifecycle/src/index.ts:81-106,352-358` | Same Research | Can an approval announcement construct a worker? | Reported by Research | No parent or assignment is present; use announcements only as wakeups |
| `plans/15-orchestrator-vision-and-control.md`, `maxConcurrentChildren` schema | Utility, earlier source verification | Which default and bounds did the cited plan specify? | Earlier direct inspection recorded in this piece | Keep configurable 4 and 1–32; do not claim measured capacity |

## How to see it

```bash
pnpm exec vitest run packages/dev-loop/queue/tests/queue.spec.ts
```

Expected result: **40 passing cases**. Parent post-transfer execution on master passed all 40 with **100% statements, branches, functions, and lines**. The cases prove reservation before dispatch, held startup and cleanup at the ceiling, dependency wakeups, ignored unrelated events, cancellation joining disposal, startup rejection, and failure-closed admission. The original 38-case baseline had 27 behavioral RED and 11 passing. Two later cancellation cases are explicitly GREEN coverage additions, not invented RED evidence.

The real-composition case must load the service from test-only YAML with a non-default ceiling and observe that many reserved operations. A later `/dev-loop` command owns operator presentation. This piece does not claim that command exists.

## Teach me while you build

**Background:** A semaphore reserves a limited number of permits. Here a reservation covers startup, execution, and cleanup, because each stage can still consume resources. Releasing at leader completion alone can exceed the intended limit while old work stays alive.

**How it works here:** The role consumer supplies an executable callback. The queue supplies admission, ordering, dependencies, captured initiator identity, and cancellation. Existing Cordis and subagent APIs provide scoped calls, removable listeners, a result promise, and a disposal promise. We do not build another subagent backend.

**Approaches considered:** A per-request callback using the existing run handle is chosen because 00.06 already owns the provider and assignment. A dispatcher registry would help if we needed multiple independently selected dispatcher implementations, but no current consumer needs that extra lifetime. Direct queue-owned spawning would force the queue to absorb role/worktree policy and would create a dependency cycle. Counting global events loses startup-rejection and cleanup information already available from the owned promises.

**Prior art inspected:** Parent directly read the [p-queue README](https://raw.githubusercontent.com/sindresorhus/p-queue/main/readme.md), its API `add`/`signal`, `onIdle`, `clear`, and Custom QueueClass, during round 38. This was documentation inspection of the moving main branch, not a pinned implementation or benchmark, and no code was reused. p-queue is a real alternative for priority/concurrency scheduling. But piece dependencies, captured Agent identity, and resource joining would stay our responsibility.

**From the docs:** Its cancellation FAQ says “it does not automatically stop the async work inside your function.” So rejecting a task promise is not evidence that its resource stopped. A library-backed implementation would still need the owned cleanup promise and tests used here.

**What to notice:** Reserve before awaiting startup, and do not free the reservation until disposal settles. Capture the initiating Agent before parking. A later lifecycle event's ambient caller is not the original delegator.

**Concepts to own:** Structured concurrency joins child work before its owner exits. Backpressure parks work instead of spawning without a bound. Stable priority scheduling uses queue number first and FIFO order for ties. These appear in dispatch lifetime, capacity reservation, and eligible-request selection respectively.

**Interview angle:** “Why can a concurrency limiter exceed its limit?” Because it counts only returned handles, or releases before cleanup. Reserve before calling the asynchronous factory, and join the returned resource before releasing. The follow-up is how to handle a factory that returns a resource after cancellation. This queue disposes and joins that late resource.

**Decisions you can make:** Change the configured limit within the documented bounds after measuring the deployment. Do not add continuable runs without a real cancellation/join owner, and do not infer an assignment from a status event. The provider and role owners must resolve those changes together.

**Lesson:** A lifecycle announcement reports an event. An owned handle provides the cancellation and settlement needed to manage capacity.

## Resources and proof

| Claim | Citation | How established | Checked against |
|---|---|---|---|
| Approval lacks executable dispatch context | Lifecycle `StateTransitionEvent`; Approval `PresentPieceResult` | Reported by Research `2c71989f-930e-4292-b974-9d67787c5ab3`; parent also read result declarations | master plus frozen 00.03, round 37 |
| One-shot startup returns result/dispose ownership; startup rejection can precede any lifecycle announcement | Subagent `start`, `SubagentRun`, and `tests/service.spec.ts:258-270` | Reported by the same Research | master, round 37 |
| Child SessionId and lifecycle run epoch are different identities | Subagent `types.ts:80-116,308-333`, `lifecycle.ts:134-162` | Reported by the same Research | master, round 37 |
| Dependencies belong to parsed records, not a lifecycle dependency API | Directory `PieceRecord.dependsOn`; Lifecycle public methods | Reported by the same Research | master, round 37 |
| The default is a convention, not measured capacity | Plan 15 schema; no capacity benchmark was run | UNVERIFIED as a performance claim; retained only as a documented convention | round 37 |
| Queue ownership and teardown work as specified | `packages/dev-loop/queue/tests/queue.spec.ts` | Parent post-transfer execution: 40 passing cases, strict 100% coverage; frozen test SHA256 `f8c2e74442fd549e2a8860d383f1e712d3b900dbb2ff364b1263a4db3a869a02` | master, round 38 |

## Reuse capture

**Reusable implementation pattern:** Hold a reservation around an asynchronous resource factory, its result, and its cleanup. Potential reuse includes subprocess admission and bounded background operations. Generalization requires another consumer with equivalent ownership. This is not a new generic task-pool library. Confidence is medium for these one-shot operations, supported by the named tests. Generic reuse remains unproven.

**Anti-pattern:** An event payload with no caller or assignment cannot become an executable request by naming an `enqueue(pieceId)` method. The missing information belongs to the dispatching consumer, not to an inferred default.

**Known limitations:** State is process-local. Restart recovery and durable revision-bound approval belong to 00.12. The limit counts only submitted one-shot operations, not every subagent or provider-internal child in the process.

## Acceptance

1. Test Writer records behavioral RED without import failures. Implementer passes frozen tests and strict source coverage. Parent reruns on master before moving and committing this piece.
2. Config defaults to 4, accepts integers 1–32, and rejects 0, 33, and fractional values at mount.
3. A real Loader composition demonstrates a non-default ceiling, including held startup and cleanup.
4. Requests require live initiator identity, pending status, canonical dependencies, and no duplicate outstanding piece id.
5. Priority/FIFO ordering, dependency wakeups, and blocked-dependency continuation are deterministic and timer-free.
6. No callback starts from a bare approval event, an unrelated subagent event, a cancelled queued request, or a disposed queue.
7. Startup failure, all run outcomes, cancellation, late startup, and fiber disposal settle exactly once. Cleanup failure is surfaced without falsely freeing capacity.
8. Public snapshots contain only owned scalar fields, and the role consumer retains provider, assignment, and worktree policy.
