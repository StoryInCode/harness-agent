# 00.06c — Subagent Spawning with Injected Context

**Set:** 00-dev-loop · **Queue:** 21 · **Depends on:** 00.06a, 00.06b
**Lead Developer:** 💻 Daru (Super Hacker)
**Status:** done
**Harness primitive:** Service Provider · **Package:** `@deepseek-ai/dsh-dev-loop-roles`

## Summary

Yo! When you spawn a subagent worker, you can't just throw them into an empty room and expect miracles. You gotta inject the exact assignment brief, the piece ID, the isolated worktree CWD, and the persona prompt! Daru hooks directly into `ctx.subagents` to spawn in-process workers with custom environments. Clean process boundaries without IPC lag!

### Human Decision Point: Option A vs Option B
- **Option A (Active)**: In-process subagent spawning with scoped CWD, persona prompt, and brief injection.
- **Option B (Alternative)**: External OS process spawning that requires complex IPC sockets and serialize/deserialize overhead.

## Behaviour

- **Given** a delegation brief targeting piece '00.06' **When** `delegate(brief, signal)` is called by an active initiator **Then** it assigns a detached worktree and starts a subagent whose session `cwd` is locked to the worktree path.
- **Given** a role policy with configured persona and toolFilter **When** `ctx.subagents.start` is invoked **Then** the child subagent receives the injected prompt with role brief and persona constraints.
- **Given** a child subagent that fails or is cancelled **When** the ticket settles **Then** `DevLoopRoles` records a settled failure record with diagnostic limitations without throwing unhandled errors to the host.

## Harness fit

Service Provider method on `ctx.devLoopRoles` in `packages/dev-loop/roles/src/index.ts` coordinating `ctx.devLoopQueue`, `ctx.devLoopWorktree`, and `ctx.subagents`.

## Contracts

```typescript
export interface DelegationBrief {
  readonly pieceId: string
  readonly role: DevLoopRole
  readonly assignment: string
  readonly rationale: string
  readonly verification?: {
    readonly claim: string
    readonly decisionRestingOnClaim: string
    readonly permittedSources: readonly string[]
    readonly requiredEvidence: readonly string[]
  }
}

export declare class DevLoopRoles extends Service {
  delegate(brief: DelegationBrief, signal: AbortSignal): Promise<SettledDelegation>
}
```

## Dependencies

- **Prerequisites:** 00.06a, 00.06b

## References

| Source | Role/preset that inspected it | Question it answered | Direct inspection or reported | How it was used |
|---|---|---|---|---|
| `packages/dev-loop/roles/src/index.ts:73-150` | 💻 Daru (Super Hacker) | How does the roles service coordinate worktree assignment and subagent dispatch? | direct inspection | Verified `delegate` and `perform` implementation using `ctx.subagents.start` |
| `packages/dev-loop/roles/src/types.ts:25-36` | 💻 Daru (Super Hacker) | What fields comprise the delegation brief input? | direct inspection | Verified `DelegationBrief` interface contract |
| `packages/dev-loop/roles/src/types.ts:67-105` | 💻 Daru (Super Hacker) | What state transitions exist between requested and settled delegations? | direct inspection | Verified `RequestedDelegation` and `SettledDelegation` types |
| `packages/dev-loop/roles/tests/roles.spec.ts:10-42` | 💻 Daru (Super Hacker) | How does the test suite verify worktree CWD locking and prompt injection? | direct inspection | Verified child session observation assertions in `Host role delegation` test |

## How to see it

Run:
```bash
pnpm exec vitest run packages/dev-loop/roles/tests/roles.spec.ts -t 'records the actual child'
```
Expected output:
```
✓ Research records the actual child, caller, assigned cwd and reported provenance after cleanup
```

## Teach me while you build

Booting up subagents is like spinning up dedicated worker threads in a game engine. Let's make it fast and clean.

- **What it does**: Spawns and monitors a child subagent configured with the appropriate role, worktree directory, and brief.
- **Background**: DSH supports multiple subagent providers (spawn-in-process, fork-in-process, ACP). This gate coordinates the spawn.
- **How it works here**: `delegate()` resolves the worktree path via `ctx.devLoopWorktree`, applies role tool filters, builds the prompt, and awaits `ctx.subagents.start()`.
- **Why this approach**: Using native in-process subagents avoids IPC serialization bugs and provides sub-millisecond turn handover.
- **Approaches considered**: Raw child_process fork (heavyweight memory) vs Shared in-process Cordis contexts (chosen: lightweight and shared objects).
- **Prior art inspected**: Actor model spawning (Erlang `spawn_link`) and Web Workers.
- **What we get for free**: DSH `ctx.subagents` provides automatic session header inheritance and cleanup.
- **From the docs**: AGENTS.md: 'Native Two-Way Brain-Worker Communication: parent calls send_message... child replies before finish.'
- **What to notice**: Notice how the child's `cwd` is explicitly pointed to the worktree to ensure file tools operate in the sandbox!
- **Concepts to own**: Context injection: providing a child execution context with scoped permissions and targeted state.
- **Interview angle**: How do you achieve workspace isolation in multi-agent orchestration? Answer: Configure child session working directory to point to an isolated worktree.
- **Decisions you can now make alone**: You can change subagent model selection policies without changing the delegation brief schema.
- **What you should learn from this**: Give your workers the exact context they need to succeed, and keep their boundaries crisp!

## Resources and proof

| Claim | Citation | How established | Checked against |
|---|---|---|---|
| Delegation binds worktree CWD and role prompt to spawned child | `packages/dev-loop/roles/src/index.ts:118-135` | direct inspection | Current checkout |
| Child dispatch joins queue ticket and cleans up on failure or abort | `packages/dev-loop/roles/src/index.ts:137-149` | direct inspection | Current checkout |
| Delegation test verifies assigned CWD and prompt contents | `packages/dev-loop/roles/tests/roles.spec.ts:10-42` | direct inspection | Current checkout |
| Test suite verifies live preset inheritance across delegations | `packages/dev-loop/roles/tests/roles.spec.ts:44-53` | direct inspection | Current checkout |

**Learning resources**

- Role delegation implementation and worktree dispatch lifecycle — `packages/dev-loop/roles/src/index.ts:73-150`
- Delegation brief contracts and input validation types — `packages/dev-loop/roles/src/types.ts:25-36`
- Requested and settled delegation record definitions — `packages/dev-loop/roles/src/types.ts:67-105`
- Role delegation test suite validating CWD locking and prompt injection — `packages/dev-loop/roles/tests/roles.spec.ts:10-53`
- Queue ticket and worker coordination contracts — `packages/dev-loop/queue/src/types.ts:1-50`

## Reuse capture

Reuse candidate: Scoped Subagent Task Dispatcher
What it does: Spawns role-specialized subagents in isolated workspaces.
Current usage: Core delegation engine in `dsh-dev-loop-roles`.
Potential reuse: Can dispatch parallel workers for documentation, testing, or linting.
What would need to become generic: Works with any Cordis subagent provider.
Evidence: packages/dev-loop/roles/src/index.ts
Confidence: high

## Acceptance

- [x] Spawns subagent in designated worktree CWD.
- [x] Injects role prompt and brief cleanly.
- [x] Returns settled outcome upon completion.
- Next Gate: [`00.06d`] — Proceed to next verified milestone.
