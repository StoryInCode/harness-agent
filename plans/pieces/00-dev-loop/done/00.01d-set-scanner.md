# 00.01d — Directory Discovery and Set Traversal

**Set:** 00-dev-loop · **Queue:** 4 · **Depends on:** 00.01a, 00.01b, 00.01c
**Lead Developer:** 🔬 Hououin Kyouma (Mad Scientist)
**Status:** done
**Harness primitive:** Service Provider · **Package:** `@deepseek-ai/dsh-dev-loop-directory`

## Summary

Mwahahaha! Fools of the Organization! Do you think you can hide fragmented pieces across disparate directories? My Directory Scanner pierces through the temporal fabric of the filesystem, traversing set folders and uncovering both active and completed timeline artifacts! It constructs the unified SetScan DAG!

### Human Decision Point: Option A vs Option B
- **Option A (Active)**: Hierarchical directory traversal reading set directories and sibling `done/` subdirectories.
- **Option B (Alternative)**: Flat directory structure that forces all completed and pending pieces into one messy folder.

## Behaviour

- **Given** a filesystem with set `00-dev-loop` containing root and `done/` files **When** scanSet is called **Then** it discovers both pending pieces and completed pieces in `done/`.
- **Given** a directory containing non-piece markdown files like `README.md` **When** scanSet executes **Then** it skips non-piece files without reporting parse errors.
- **Given** a piece with invalid syntax inside the directory **When** scanSet runs **Then** it places the piece into `rejected` list while continuing to process valid pieces.

## Harness fit

Cordis Service Provider implementing `ctx.devLoopDirectory` in `packages/dev-loop/directory/src/index.ts`. Exposes `scanSet` and `listSets` to the Host Plane.

## Contracts

```typescript
export interface PieceRejection {
  readonly path: string
  readonly code: PieceFindingCode
  readonly findings: readonly PieceFinding[]
}

export interface SetScan {
  readonly pieces: readonly PieceRecord[]
  readonly rejected: readonly PieceRejection[]
}

export interface DevLoopDirectory {
  scanSet(setName: string, signal?: AbortSignal): Promise<SetScan>
  listSets(signal?: AbortSignal): Promise<string[]>
}
```

## Dependencies

- **Prerequisites:** 00.01a, 00.01b, 00.01c

## References

| Source | Role/preset that inspected it | Question it answered | Direct inspection or reported | How it was used |
|---|---|---|---|---|
| `packages/dev-loop/directory/src/index.ts:230-251` | 🔬 Hououin Kyouma (Mad Scientist) | How does directory scanning traverse root and done folders while aggregating rejections? | direct inspection | Modeled scanSet method behavior and traversal structure |
| `packages/dev-loop/directory/src/types.ts:89-105` | 🔬 Hououin Kyouma (Mad Scientist) | What data contracts represent valid set scans and malformed piece rejections? | direct inspection | Defined SetScan and PieceRejection contracts |

## How to see it

Run:
```bash
pnpm exec vitest run packages/dev-loop/directory/tests/directory.spec.ts -t 'merges the set directory and its done/ subdirectory into one queue-ordered list'
```
Expected output:
```
✓ scanning a set directory > merges the set directory and its done/ subdirectory into one queue-ordered list
```

## Teach me while you build

The lab's radar must scan every worldline! Let me explain how filesystem traversal avoids the traps of the Organization!

- **What it does**: Discovers all piece files in a given set folder and its `done/` subdirectory, parsing them into valid or rejected records.
- **Background**: In DSH, active pieces live in plans/pieces/<set>/ while completed pieces move to plans/pieces/<set>/done/.
- **How it works here**: `scanSet` uses `ctx.fs.listDir` to list files. It checks each against `isPieceFilename`. Valid pieces are parsed; failures go to `rejected`.
- **Why this approach**: Isolating `done/` in a subdirectory prevents active work queries from being polluted while keeping the audit trail complete.
- **Approaches considered**: Recursive deep globbing (rejected: slow on large trees) vs Targeted two-tier scan (chosen: scans set root + `done/` only).
- **Prior art inspected**: Maildir directory format: separating `new`, `cur`, and `tmp` mail files into subdirectories.
- **What we get for free**: `ctx.fs` abstraction allows this service to run over real POSIX disk, memory FS, or Git worktrees seamlessly.
- **From the docs**: plans/AGENTS.md: 'A piece that is complete does not stay in the plan set. It moves to a done/ folder.'
- **What to notice**: Notice that one malformed piece in a set never throws an unhandled exception; it is safely quarantined in `rejected`.
- **Concepts to own**: Fault-tolerant directory scanning: preserving system availability in the presence of corrupted individual files.
- **Interview angle**: How do you prevent one corrupt configuration file from crashing an entire daemon? Answer: Partition scan results into accepted and rejected buckets.
- **Decisions you can now make alone**: You can safely add custom metadata files (e.g. `.notes`) to set directories without failing piece scans.
- **What you should learn from this**: A resilient scanner isolates malformed inputs so valid work continues unimpeded. El Psy Kongroo.

## Resources and proof

| Claim | Citation | How established | Checked against |
|---|---|---|---|
| Directory scanner collects rejected pieces without aborting scan | `packages/dev-loop/directory/src/index.ts:230-251` | direct inspection | Current checkout |
| Service publishes ctx.devLoopDirectory on the Cordis context | `packages/dev-loop/directory/src/index.ts:182-206` | direct inspection | Current checkout |
| SetScan contract separates valid pieces from parse rejections | `packages/dev-loop/directory/src/types.ts:89-105` | direct inspection | Current checkout |

**Learning resources**

- Directory service provider implementation — `packages/dev-loop/directory/src/index.ts`
- Micro-gate canonical layout and line limit rules — `plans/pieces/PIECE-FORMAT.md`
- Cordis service mounting, lifecycle, and injection patterns — `docs/cordis-primer.md`
- Development loop directory rules and axioms — `plans/AGENTS.md`
- Maildir directory separation specification — https://cr.yp.to/proto/maildir.html

## Reuse capture

Reuse candidate: Fault-Tolerant Two-Tier Directory Scanner
What it does: Scans root and done folders, collecting valid records and rejected errors.
Current usage: Core intake engine of the Dev Loop.
Potential reuse: Can be adapted for task runners, migration folders, or inbox processors.
What would need to become generic: Filename matcher and parser can be injected as parameters.
Evidence: `packages/dev-loop/directory/src/index.ts:230-251`
Confidence: high

## Acceptance

- [x] Discovers both active and done pieces.
- [x] Quarantines malformed pieces into `rejected` list.
- [x] Registers as a singleton Cordis service `ctx.devLoopDirectory`.
- Next Gate: [`00.01e`] — Proceed to next verified milestone.
