# 00.09b — Behavioral RED Baseline Test Execution

**Set:** 00-dev-loop · **Queue:** 27 · **Depends on:** 00.09a
**Lead Developer:** 💻 Daru (Super Hacker)
**Status:** todo
**Harness primitive:** Service Provider · **Package:** `@deepseek-ai/dsh-dev-loop-test-handoff`

## Summary

Listen up, noobs! Anyone can write a test that passes because it tests nothing (`expect(true).toBe(true)` LOL). True hackers require a real RED baseline. The Test Writer writes test files targeting the unwritten code, and we RUN them. They MUST fail on assertion errors, NOT on syntax crashes or missing imports. That proves the test actually exercises the target behavior!

### Human Decision Point: Option A vs Option B
- **Option A (Active)**: Execute test suite in isolated worktree and verify failure is caused by behavioral assertion errors.
- **Option B (Alternative)**: Accept test files without executing them, letting syntax errors or trivial passes slip into master.

## Behaviour

- **Given** a freshly authored test suite that fails with `AssertionError: expected false to be true` **When** runBaseline(worktree) is executed **Then** it confirms a valid behavioral RED baseline.
- **Given** a test suite that fails because of a TypeScript syntax error or missing module import **When** runBaseline runs **Then** it rejects the baseline with `SetupFailureError`.
- **Given** a test suite where all tests unexpectedly pass before implementation **When** runBaseline runs **Then** it rejects with `VacuousPassError`.

## Harness fit

Service Provider method on `ctx.devLoopTestHandoff` in `@deepseek-ai/dsh-dev-loop-test-handoff/src/runner.ts`.

## Contracts

```typescript
export interface RedBaselineResult {
  readonly totalTests: number
  readonly failedCount: number
  readonly isBehavioralRed: boolean
  readonly failureOutput: string
}

export interface DevLoopTestHandoff {
  runBaseline(worktreePath: string, testFiles: readonly string[]): Promise<RedBaselineResult>
}
```

## Dependencies

- **Prerequisites:** 00.09a

## References

| Source | Role/preset that inspected it | Question it answered | Direct inspection or reported | How it was used |
|---|---|---|---|---|
| `packages/dev-loop/gates/src/runner.ts:5-18` | 💻 Daru (Super Hacker) | How does the harness classify test outcomes into assertion RED versus syntax errors? | direct inspection | Verified `RunnerOutcomeClass` taxonomy and `classifyRun` interface |
| `packages/dev-loop/gates/tests/runner.spec.ts:31-61` | 💻 Daru (Super Hacker) | How does the test suite verify rejection of syntax errors and empty suites? | direct inspection | Examined test cases distinguishing assertion-red from syntax-or-import-error |
| `packages/dev-loop/gates/src/handoff-snapshot.ts:98-131` | 💻 Daru (Super Hacker) | What execution facts and baseline evidence compose `FrozenRedEvidence`? | direct inspection | Aligned `RunFacts` and `FrozenRedEvidence` contracts with test handoff runner |
| `packages/dev-loop/gates/tests/red.spec.ts:18-50` | 💻 Daru (Super Hacker) | How does the RED evaluation gate verify and enforce valid behavioral RED evidence? | direct inspection | Checked gate evaluation assertions for missing, failed, and valid RED evidence |

## How to see it

Run:
```bash
pnpm exec vitest run packages/dev-loop/test-handoff/tests/runner.spec.ts
```
Expected output:
```
✓ runBaseline > distinguishes assertion failures from syntax errors
```

## Teach me while you build

Exit code 1 doesn't automatically mean RED. If node crashed, your test isn't red; it's broken. Learn the difference.

- **What it does**: Executes the newly authored test files to confirm they fail exclusively on genuine assertions.
- **Background**: A vacuous test is one that passes even when the feature is completely absent. A broken test fails to compile.
- **How it works here**: Spawns Vitest with the new test files. Inspects stdout/stderr and JSON reporter. Requires exit code non-zero with `AssertionError`.
- **Why this approach**: Guarantees that when the Implementer later gets the tests green, real functionality was actually created.
- **Approaches considered**: Static AST analysis of tests (cannot prove runtime failure) vs Real execution under Vitest (chosen: empirical proof).
- **Prior art inspected**: Mutation testing (Stryker) and TDD red-phase enforcement.
- **What we get for free**: Vitest JSON reporter provides machine-readable test failure reasons and call stacks.
- **From the docs**: docs/testing.md: 'Tests describe behavior, not correctness... match evidence to the surface.'
- **What to notice**: Notice how syntax errors and import errors are rejected immediately so the Test Writer must fix setup!
- **Concepts to own**: Behavioral failure vs Setup failure: proving a test asserts intended semantics rather than accidental breakage.
- **Interview angle**: How do you automate verification that a test suite is not vacuous in an autonomous coding agent? Answer: Execute test before implementation, verify assertion failures.
- **Decisions you can now make alone**: You can configure test timeout limits during baseline execution.
- **What you should learn from this**: A green test is only as trustworthy as the red baseline that preceded it. Hack with proof!

## Resources and proof

| Claim | Citation | How established | Checked against |
|---|---|---|---|
| Runner outcome classification distinguishes behavioral assertion failures from syntax and import errors | `packages/dev-loop/gates/src/runner.ts:5-18` | direct inspection | Current checkout |
| Unit tests verify classification of assertion-red, timeout, and syntax error run facts | `packages/dev-loop/gates/tests/runner.spec.ts:31-61` | direct inspection | Current checkout |
| FrozenRedEvidence binds immutable execution facts and expected failure assertions | `packages/dev-loop/gates/src/handoff-snapshot.ts:98-131` | direct inspection | Current checkout |

**Learning resources**

- Vitest JSON reporter and programmatically inspectable test results — https://vitest.dev/guide/reporters.html#json-reporter
- Runner outcome classification interface and taxonomy — `packages/dev-loop/gates/src/runner.ts:5-18`
- Test specifications verifying classification of assertion failures vs setup errors — `packages/dev-loop/gates/tests/runner.spec.ts:31-61`
- RED baseline evidence verification and gate evaluation tests — `packages/dev-loop/gates/tests/red.spec.ts:18-50`

## Reuse capture

Reuse candidate: Behavioral RED Baseline Validator
What it does: Verifies that tests fail on assertions before coding starts.
Current usage: Used in `dsh-dev-loop-test-handoff`.
Potential reuse: Can gate pull requests or verify student assignments.
What would need to become generic: Can adapt to any test runner that outputs JSON.
Evidence: packages/dev-loop/test-handoff/src/runner.ts
Confidence: high

## Acceptance

- [ ] Runs tests in isolated worktree.
- [ ] Confirms behavioral assertion failures.
- [ ] Rejects compile errors and vacuous passes.
- Next Gate: [`00.09c`] — Proceed to next verified milestone.
