# 00.06 — Role Delegation Dispatcher and Record

**Set:** 00-dev-loop · **Queue:** 6 · **Depends on:** 00.04, 00.05
**Status:** todo
**Harness primitive:** subagent consumer · **Package:** `@deepseek-ai/dsh-dev-loop-roles`

## Summary

Delegate bounded assignments to Research, Test Writer, Implementer, or Utility, run each child in its piece's retained worktree, and retrieve durable delegation history. The Brain calls registered model tools; a persona cannot invoke Host services merely by naming them. Reports preserve assignment, rationale, actual child identity and composition, outcome, limitations, and explicitly attributed evidence.

This specification proposes a Host service and a scoped `./tool` consumer in one package. Implementation follows the separately frozen child-cwd prerequisite below. Role acceptance remains proposed. The child-cwd prerequisite has independently executed RED/GREEN and post-transfer evidence below; that does not complete the Role package.

## Behaviour

- **Given** an admitted piece **When** the Brain calls `dev_loop_delegate` **Then** persist its request before enqueueing, allocate or reuse its assignment after admission, and start one child with the real caller and assigned cwd.
- **Given** Research **When** its child executes **Then** inherited write-capable tools are excluded from both model schemas and execution, while its brief requests citations and limitations.
- **Given** Test Writer followed by Implementer **When** each role finishes **Then** the same assignment and Test Writer files remain; role cleanup never retires the tree.
- **Given** a settled run **When** Queue finishes cleanup **Then** persist the terminal observation before returning a successful delegation receipt.
- **Given** cancellation, startup failure, or uncertain cleanup **When** delegation settles **Then** record the observed failure and limitations without claiming completion, quiescence, or deletion.
- **Given** an unknown role, including `Reviewer` **When** delegation is requested **Then** reject with `UNKNOWN_DELEGATION_ROLE`; no fifth role or alias is introduced.
- **Given** a restarted service **When** history is queried **Then** return durable records in deterministic chronological order; an unfinished request remains unresolved rather than being declared running or completed.
- **Given** a delegated research report **When** another agent summarizes it **Then** attribute it to the recorded role and actual composition, never to the summarizer's direct inspection.

## Harness fit

The Host service publishes `ctx.devLoopRoles`, injecting `agents`, `subagents`, `devLoopQueue`, `devLoopWorktree`, and `storageDomain`. Its `./tool` function plugin injects `tools`, `agents`, and `devLoopRoles`, and registers effect-owned tools only in the Brain's scoped composition. Do not mount one Host service or storage domain per role. The service is a consumer of `SubagentRuntime.start(providerName, request)`, not an alternative subagent provider.

Use `ctx.subagents.start`, which owns capability checks, descriptor resolution, catalog publication, and lifecycle observation. Do not call a provider's raw `start` to bypass those checks. Provider registry names are configuration, not LLM model-route identifiers. The consumer supplies `persona` and `toolFilter`; the provider applies them during child creation. No consumer callback receives a `childCtx`.

### Frozen prerequisite: explicit child cwd

This prerequisite is part of 00.06, not a new piece. Its Test Writer freezes behavioral RED independently before implementation. Changes to its scope or expectations require an explicit new baseline; role implementation must not silently expand it.

**Desired behavior:** create the actual child with the assigned absolute workspace in immutable `SessionHeader.cwd`, before setup, publication, and the first model request. Preserve the exact parent's identity, cwd, header, and sibling metadata. Never mutate a parent header, manufacture a caller, or call `process.chdir`.

**Smallest public amendment:** add optional `readonly cwd?: string` to `SubagentStartRequest`. `ResolvedSubagentStartRequest` inherits it alongside its existing descriptor; “resolved” does not imply that the service resolves provider-owned directory defaults. At each existing request-to-creation/run-spec step, select explicit request cwd, then the provider's configured cwd where supported, then the real parent cwd. Invalid explicit input rejects without fallback. Omission preserves existing behavior, including undefined in-process cwd and remote providers' required usable directory. Validate paths in the provider's execution world, not by adding generic Host filesystem probes.

Pass the selected cwd into `childSessionMeta`; do not leave that helper to reread the parent workspace. All current providers must honor cwd as a baseline request field, so no capability bit is added. Existing Agent creation already accepts `meta.cwd` and snapshots metadata before setup; no Agent-loop change, generic `meta`/`setup` request escape, or Session format bump is needed.

| Existing extension considered | Why it cannot supply this behavior |
|---|---|
| Request persona, toolFilter, agentOptions, outputSchema, maxDepth | These select prompt, capabilities, route, result, or depth, not creation metadata. |
| Prompt/skill instructions, shell workdir, filesystem resolution options | They affect chosen operations, not the durable header, other tools, model cwd, or sandbox default. |
| Tool wrappers, guards, pre-execute hooks | They act on execution, not the provider's unpublished session creation input. |
| Queue dispatch and WorktreeAssignment | They provide admission and a directory, but the existing request cannot carry that directory. |
| Preset mount/composeFrom/recompose/select | They select composition, not workspace; a preset's tool cwd does not replace Session cwd. |
| Subagent/Agent publication events | Observers see an already-created child, too late to change immutable metadata safely. |
| AgentRegistry.create({meta, setup, parentAgent}) | Sufficient underlying primitive, but existing providers own its invocation; the role consumer cannot inject setup. |
| ACP/DSH SDK configured cwd | Static deployment choices cannot select per-piece local role workspaces, and these providers lack persona/toolFilter support. |
| Adjacent provider or wrapping an existing provider | A wrapper has no cwd channel; a new provider duplicates lineage, delegated policy, composition, structured capture, result, and cleanup ownership. Parent-keyed side maps introduce cross-request races. |

The missing input therefore belongs in the existing subagent request/provider implementation, not an adjacent policy plugin. Retain existing AgentFactory and transport machinery.

| Required cwd owner/consumer | Amendment and proof scope |
|---|---|
| `subagent/src/types.ts`, `child-agent.ts`, `subagent-in-process-driver/src/index.ts` | Document request/resolved semantics; select cwd once and stamp real creation metadata. Spawn and fork forward unchanged through the shared driver; prove both. |
| `subagent/src/continuation.ts` | Its request derives from SubagentStartRequest: honor cwd during initial creation before awaits; cold resume retains persisted child cwd. Provider preparation contributes seed only. |
| `subagent/src/out-of-process.ts` | Extend resolveChildCwd precedence without changing configured/parent omission behavior. |
| `subagent-acp/src/index.ts`, `subagent-dsh-sdk/src/index.ts` | Update private resolveCwd/SDK start resolution; align SDK's provider request annotation with the resolved request. |
| `subagent-codex/src/index.ts`, `subagent-claude-code/src/index.ts` | Use requested cwd; remove the unconditional requirement that parent cwd exist when an explicit child cwd is valid. |
| `subagent-antigravity/src/index.ts` | Feed requested cwd into its existing run spec. |
| Existing remote run/process/wire consumers | They already consume run-spec cwd; prove the actual subprocess, SDK query, and wire inputs, not merely a resolver result. |
| tool-subagent and workflow-worker-thread request builders; Ralph | Optional omission keeps inheritance; no new model cwd option is required. Update directly constructed resolved fixtures only where their types change. |
| FS/shell/LSP/skill/instruction tools, sandbox, persistence, workspace/query/API readers | Continue consuming the real child header; do not add role-specific overrides or rewrite persistence formats. |

Prerequisite proof must cover explicit/omitted/invalid cwd, valid explicit cwd without parent cwd, parent/sibling immutability, actual spawn/fork headers and model cwd, continuable initial creation/cold resume, and all five external adapters. Use actual provider implementations with private fixtures and only external endpoints simulated. New source changes need owner documentation and applicable generated-consumer updates, not an exhaustive framework audit.

## Contracts

The following API is proposed. JSON boundaries validate before branding opaque ids; no live Harness object is stored.

```typescript
export type DevLoopRole = 'Research' | 'Test Writer' | 'Implementer' | 'Utility'
export interface VerificationAssignment {
  claim: string
  decisionRestingOnClaim: string
  permittedSources: readonly string[]
  requiredEvidence: readonly string[]
}
export interface DelegationBrief {
  pieceId: string
  role: DevLoopRole
  assignment: string
  rationale: string
  verification?: VerificationAssignment
}
export interface RoleConfig {
  provider: string
  persona: string
  toolFilter: ToolRestriction
  agentOptions?: AgentOptions
  maxDepth?: number
}
export interface Config {
  roles: Record<DevLoopRole, RoleConfig>
  maxBriefBytes: number
  maxOutcomeBytes: number
}
export interface ToolConfig {
  maxToolOutputBytes: number
  maxHistoryRecords: number
}
export interface RequestedDelegation extends DelegationBrief {
  delegationId: DelegationId // Branded<'DevLoopDelegationId'>
  parentSessionId: SessionId
  provider: string
  requestedAt: number
  state: 'requested'
}
export interface SettledDelegation extends Omit<RequestedDelegation, 'state'> {
  state: 'settled'
  finishedAt: number
  subagentSessionId?: SessionId
  effectivePreset?: string
  worktreeAssignment?: WorktreeAssignment
  stopReason?: SubagentStopReason
  status: 'completed' | 'aborted' | 'failed'
  cleanup: 'quiescent' | 'unproven'
  outcome: string
  limitations: readonly string[]
  provenance: { kind: 'reported'; role: DevLoopRole; preset?: string }
}
export type DelegationRecord = RequestedDelegation | SettledDelegation
export interface DevLoopRoles {
  delegate(brief: DelegationBrief, signal: AbortSignal): Promise<SettledDelegation>
  getDelegations(pieceId: string): Promise<readonly DelegationRecord[]>
}
```

### Configuration, tools, and actual composition

The Host service owns `Config` (role policies and brief/outcome retention limits). The scoped `./tool` consumer owns its separate validated `ToolConfig` (complete rendered-output bytes and history record count); it must not read the service's private configuration. Require all four role configurations and positive safe-integer limits, validated at their owning mounts with no invented deployment defaults. Validate nonempty provider/persona and nonempty tool-filter configuration; resolve missing providers and unsupported capabilities at the earliest resolvable point and again through the service at start. Research permits only configured read-only capabilities; general execution and further-delegation tools must be excluded unless another real enforcement owner bounds their effects. Bounds cover UTF-8 bytes of complete owned JSON/rendered results, including metadata, provenance, and wrappers, not just assistant text.

Register `dev_loop_delegate` with the JSON fields of DelegationBrief and `dev_loop_delegations` with `{pieceId}` through `ctx.tools.register(defineTool(...))`, declaring parameters, output schema, and a pure renderer. Neither accepts provider, cwd, preset, parent, signal, persona, or toolFilter from the model. Use existing generic tool cards unless a separate pure presenter is justified; model-visible output is logged by the tool runtime. No new SessionEventMap member is needed for this tool-owned result path.

Reject oversized briefs before any request record or enqueue. If provider output exceeds maxOutcomeBytes or cannot be represented as text, record a bounded failed observation with an explicit limitation; never present a clipped report as complete. Tool output exceeding maxToolOutputBytes returns an explicit bounded error naming the durable delegation id instead of silently truncating its record. History tool output returns the complete ordered array only when both maxHistoryRecords and maxToolOutputBytes permit it; otherwise report overflow without a partial array. The service retains and returns full history. Validate configured output budgets can represent the fixed error envelopes; test tiny, exact, and multibyte boundaries. These limits do not promise bounded provider buffering or automatic history deletion.

There is no arbitrary brief.preset. The child optionally inherits the parent's live standing composition through `agentPresets.composeFrom`; a rosterless parent is supported. Capture actual `run.localAgent.session.header.agentPreset` while the run is live, or omit it when unavailable. Do not substitute a requested label or stale parent header. Provenance renders as reported by the role and actual preset, or explicitly states that no preset was recorded.

`ToolRuntime.restrict` removes inherited host/preset capabilities from schemas and real executor dispatch, including nested PTC calls. Filters intersect; unknown and reserved transport names reject. Tools registered in the child's own scope remain visible, including structured-result capture. This is not a security fence against trusted plugins, service calls, or filesystem effects of an allowed shell. Persona text is instructions, not enforcement. Allocation alone is not confinement: sandbox policy consumes the real cwd, workspace-write retains temp allowances, danger-full-access bypasses containment, and direct subprocess calls do not imply sandbox enforcement.

### Caller, Queue, and assignment ownership

At tool entry, require `exec.agent` and exact live registry equality, then invoke the service under `ctx.agents.withInitiator(exec.agent, ...)` with `exec.signal`. At service entry, capture `callerAgent = ctx.agents.requireInitiator()` before awaiting and require `ctx.agents.get(callerAgent.id) === callerAgent`. Derive its Session once. Restore that exact initiator around enqueue after persistence; never reconstruct a replacement Agent by id.

Call `queue.enqueue({pieceId, signal, dispatch})`. Its callback receives the exact captured Agent and combined cancellation. Inside admitted dispatch, require parent cwd, call `assignWorktree({pieceId,cwd,signal})`, construct the role brief, and call `subagents.start(configuredProvider,{parent:agent,signal,cwd:assignment.worktreePath,prompt,persona,toolFilter,...})`. Capture only needed run id/composition leaves and return the actual SubagentRun immediately. Do not await its result, dispose it, write a post-start storage record before handing it to Queue, or return a continuable receipt.

Queue owns result observation, disposal, and late-start cleanup through quiescence. The service awaits ticket.result; it does not claim cleanup succeeded when Queue rejects for uncertain cleanup. Cancellation after startup must cancel/join the ticket. Service disposal stops admission and joins all owned operations before closing storage. Worktree assignment survives every role result, cancellation, failure, and plugin disposal. Only the orchestration owner may retire it after all users stop and transfer/post-transfer verification finish; borrowed/dirty/changed-HEAD preservation remains 00.05's authority.

### Durable history before 00.12

The Roles service owns one `defineDomain({name:'dev-loop-roles',version:1,layout:'single',tables:{delegations:domainTable(...)}})` using a Zod discriminated record schema. Open it in Service initialization through `ctx.storageDomain.open`, retain `domain.table('delegations')`, and close it through an owned disposer after role operations settle. DomainFacility enforces single-open ownership. Authoritative invalid records reject opening; do not use backup-and-skip.

The Host composition supplies existing storage, a durable backend, and storage-domain routing. For the JSON backend, storage-json Config.root is explicit and storage-domain Config.backend names `json`; Roles neither invents a storage file nor accesses that backend directly. `KvTable.put/update` await backend durability before memory publication and domain/changed. That event is a runtime notification, not a Session event or a second persistence mechanism.

Persist a requested row before enqueue; storage failure prevents startup. After ticket result/cleanup, persist a settled observation before returning it. Expected startup, cancellation, and child failures produce truthful terminal records where persistence succeeds; inability to prove quiescence sets cleanup to unproven. Preserve the original failure independently of a terminal-write failure. Recording uses the domain write lifetime, not an already-aborted caller signal; domain writes accept no AbortSignal. Close storage only after final recording attempts, because close rejects new writes while draining already-queued writes.

Record only actual acquired assignment, child id, preset, and stop reason. A pre-start failure has no child id. Storage and child startup are not one transaction: a crash can leave a requested row with no recorded child id, and a failed terminal write leaves that prior row intact. Reopening displays unresolved requested records without fabricating activity, completion, or automatic retry. 00.12 owns later recovery/reconciliation and consumes this service rather than becoming its dependency or reopening its domain.

`getDelegations` filters table.entries by canonical piece id, orders by requestedAt then delegationId, and returns detached owned DTOs. Records include repository/worktree metadata because a piece id alone does not identify a directory; the service uses the one-repository scope of its configured allocator. Do not expose another repository's history through an unscoped global model tool. The chosen Host composition owns that repository association; cross-repository routing and automatic history retention are outside this piece.

## Dependencies

- **00.04:** Queue captures the exact initiator, admits startup callbacks, and owns run result/disposal. It does not select provider or own assignment retirement.
- **00.05:** Retained piece assignment supplies worktree path/base/ownership through RED, GREEN, transfer, and post-transfer tests; child metadata belongs here.
- **Existing Harness:** subagent providers, Agent factory, tool registry, and storageDomain with a durable backend. Provider route and composition referents must fail loud when unavailable.
- **00.11:** Human commands inspect/control the loop; they are not this model's delegation entry. **00.12:** Later loop recovery consumes existing durable records; no dependency cycle is introduced.

## References

Source links below resolve from this plan, not from a future package. Research opened the cited implementations; the orchestrator receives reported findings rather than claiming its own inspection. The delegated Research preset id was not exposed and is not invented.

| Source | Role/preset and provenance | Decision informed |
|---|---|---|
| [Subagent request types](../../../packages/subagent/subagent/src/types.ts), [runtime](../../../packages/subagent/subagent/src/index.ts) | Delegated Research, preset not recorded; reported source inspection | Real one-shot API and supported options |
| [Queue types](../../../packages/dev-loop/queue/src/types.ts), [implementation](../../../packages/dev-loop/queue/src/index.ts) | Same Research | Exact initiator and lease ownership |
| [Amended worktree piece](done/00.05-worktree-assignment.md), [human commands](00.11-command-surface.md) | Same Research | Assignment retention and missing model entry |
| [Plan axioms](../../AGENTS.md) | Supplied workspace instructions | Truthful attribution and freeze-before-implementation |

## How to see it

The Role observations below are proposed; prerequisite execution is reported separately. After Test Writer freezes RED and implementation reaches GREEN, mount the actual service and scoped tool consumer in a test-only Loader composition. A scripted Brain should call:

```json
{"pieceId":"00.06","role":"Research","assignment":"Verify the cited implementation claim","rationale":"Resolve the dependency before editing"}
```

Submit this object to `dev_loop_delegate`, then `{ "pieceId": "00.06" }` to `dev_loop_delegations`. Expect a durable settled id, actual child cwd/preset facts, and reported provenance; after service restart the same record remains. Missing providers, invalid roles, bounds overflow, or storage failure must produce explicit errors rather than fabricated success.

The proposed role test command is `pnpm exec vitest run packages/dev-loop/roles/tests/roles.spec.ts`; the Loader/restart scenario must also run through its actual owner test. The prerequisite is observable now with `pnpm exec vitest run packages/subagent`: expect 920 passing cases, including actual child headers/model input and all current provider dispatch paths. Expected named assertions: assigned durable child cwd reaches model; denied tool body never runs; Queue joins cleanup; Test Writer files survive Implementer handoff; cancelled record survives restart; requested-only crash residue stays unresolved. The implementer records exact runnable owner commands and outcomes once those tests exist.

## Teach me while you build

- **What it does:** The Brain gives a specialist a bounded job, keeps its work directory for the next specialist, and saves what actually happened.
- **Background:** A provider starts a child; a consumer chooses how to use it. An immutable Session header records creation facts. A durable domain stores application records independently of whether a model turn is open.
- **How it works here:** A registered tool supplies real caller/cancellation, Roles saves intent, Queue admits creation, Worktree supplies the retained path, and subagents starts the child. Queue cleans the run before Roles persists its terminal observation. Failures remain distinguishable from completion.
- **Why this approach:** Workspace metadata belongs at creation, execution cleanup belongs to Queue, and assignment retirement belongs to the piece owner. Sharing one lifetime would either delete handoff files or publish success before cleanup.
- **Approaches considered:** Dynamic role options over existing spawn/fork reuse proven lifecycle machinery. Separate static presets win when entire plugin composition differs, but cannot be selected through the existing request. A new provider could own metadata but duplicates unrelated lifecycle work. An event-sourced history is an alternative when the Session log owns all queries; this piece instead needs a piece-wide durable store before 00.12. No external implementation was inspected; no external comparison or code reuse is claimed.
- **Prior art inspected:** In-repository AgentPresets composition inheritance, WorkspaceRegistry's storageDomain initialization, and tool-present's defineTool registration. External prior-art inspection is not part of this bounded amendment and remains unverified.
- **What we get for free:** The tool runtime logs model-facing results; providers create children and apply supported options; Queue joins leases; storageDomain serializes durable writes and validates reopened records. Roles owns role policy, truthful records, bounds, and caller-to-piece coordination, not process isolation or crash recovery.
- **From the source documentation:** Domain.close promises to “reject new writes immediately, drain already-queued writes”; close storage after terminal recording, not before cancelling workers. See the domain interface cited below.
- **What to notice:** A filtered inherited tool must fail through the executor, not merely disappear from the prompt. A requested record after restart proves accepted intent, not a currently running child.
- **Concepts to own:** Resource ownership separates run, assignment, and storage lifetimes; commit-point publication reports a record only after durable write; nominal typing prevents confusing child and delegation ids; non-atomic side effects explain unresolved crash residue.
- **Interview angle:** “How do you avoid losing delegated work on cancellation?” Join the run, retain its workspace, and independently persist the observed result. Follow-up: a crash between independent systems requires reconciliation rather than an invented transaction.
- **Decisions you can now make alone:** Adjust validated role policy and byte budgets with focused tests. Escalate arbitrary preset selection, repository routing, child-local security enforcement, transfer/deletion, or recovery changes to their owners.
- **What you should learn from this:** Tool visibility, directory allocation, durable identity, cleanup, and evidence attribution are separate guarantees; one does not prove another.

## Resources and proof

Source-inspection evidence is reported by delegated Research; explicitly identified execution rows record runtime proof separately. Checked against the working tree during this amendment; line numbers identify inspected ranges, while symbols remain authoritative if concurrent prerequisite work moves them.

| Claim | Exact source/symbol | How established |
|---|---|---|
| Public one-shot start exists and checks capabilities | [SubagentRuntime.start/assertCapabilities](../../../packages/subagent/subagent/src/index.ts):556–585,641–647 | Reported source inspection |
| Request supports explicit cwd, persona/filter, but no preset selector | [SubagentStartRequest/ResolvedSubagentStartRequest](../../../packages/subagent/subagent/src/types.ts) | Cwd prerequisite implemented and executed; optional preset inheritance is unchanged |
| Child metadata inherits cwd; composition optionally joins live preset | [childSessionMeta/applyChildComposition](../../../packages/subagent/subagent/src/child-agent.ts):138–155,199–217; [composeFrom/composedPreset](../../../packages/preset/agent-presets/src/index.ts):451–498 | Reported source inspection |
| Factory accepts durable metadata before setup/publication | [CreateAgentOptions](../../../packages/core/agent/src/index.ts):62–118 | Reported source inspection |
| Spawn/fork share driver; continuation owns initial metadata | [startInProcessRun](../../../packages/subagent/subagent-in-process-driver/src/index.ts):104–143; [startContinuable](../../../packages/subagent/subagent/src/continuation.ts):105–171 | Reported source inspection |
| External cwd owners need explicit request handling | [resolveChildCwd](../../../packages/subagent/subagent/src/out-of-process.ts):134–152; [ACP resolveCwd](../../../packages/subagent/subagent-acp/src/index.ts):132–174; [SDK start](../../../packages/subagent/subagent-dsh-sdk/src/index.ts):144–174; [Codex start](../../../packages/subagent/subagent-codex/src/index.ts):73–96; [Claude start](../../../packages/subagent/subagent-claude-code/src/index.ts):83–111; [Antigravity start](../../../packages/subagent/subagent-antigravity/src/index.ts):60–65 | Reported source inspection |
| Queue captures exact caller and disposes returned lease | [enqueue/run](../../../packages/dev-loop/queue/src/index.ts):74–98,183–219; [QueueRequest/QueueTicket](../../../packages/dev-loop/queue/src/types.ts):13–39 | Reported source inspection |
| Filter applies to inherited tools and real executor | [restrict/view/resolveExecution](../../../packages/core/tools/src/index.ts):1054–1086,1120–1215,1536–1539 | Reported source inspection; child-local exemption explicit |
| Real header controls filesystem/model/sandbox cwd | [sessionCwd](../../../packages/fs/tool-fs/src/session-cwd.ts):22–43; [cwd variable](../../../packages/core/agent-loop/src/index.ts):423; [SandboxPolicy.resolve](../../../packages/sandbox/sandbox-policy/src/index.ts):163–168 | Reported source inspection |
| Domain owns durable writes and caller-owned close | [DomainFacility.open](../../../packages/storage/storage-domain/src/index.ts):84–103; [KvTable/Domain](../../../packages/storage/storage-domain/src/domain.ts):1–8,42–118; [defineDomain/domainTable](../../../packages/storage/storage-domain/src/spec.ts):91–107 | Reported source inspection |
| Existing Host owner opens/closes domain; backend owns files | [WorkspaceRegistry.init](../../../packages/workspace/workspace/src/index.ts):91–122; [storage-json Config/backend](../../../packages/storage/storage-json/src/index.ts):1–35,64–79 | Reported source inspection |
| Model tool registration and output are real APIs | [tool-present apply](../../../packages/fs/tool-present/src/index.ts):38–75; [ToolExecutionInput](../../../packages/core/tools/src/index.ts):307–329 | Reported source inspection |
| Preset/model/header tests provide an owner precedent | [preset-inheritance tests](../../../packages/subagent/subagent-in-process-driver/tests/preset-inheritance.spec.ts):70–132 | Source inspected, not executed |
| Roles, durable restart, and bounds work | Required role tests below | **UNVERIFIED — Role tests remain proposed** |
| Child-cwd creation and provider dispatch work | `pnpm exec vitest run packages/subagent` | Parent post-transfer: 42 suites, 920 passing cases after fixing a leaked fixture session lock. Original cwd baseline: 36 behavioral RED, 11 omission controls; all 47 then GREEN. Implementer measured 100% across 13 changed runtime/JSDoc sources. |

### Learning resources

Read SubagentStartRequest and CreateAgentOptions first to distinguish delegation inputs from creation metadata. Then read QueueRequest for run ownership, KvTable/Domain for publication and close ordering, and ToolRuntime.view for the inherited-tool exemption. The amended 00.05 owns worktree preservation and confinement qualifications.

## Reuse capture

- **Reuse candidate:** Role dispatch with durable evidence and retained assignments. Potential use: other bounded engineering handoffs. Generalization would require another real consumer and explicit repository/recovery ownership. Evidence: proposed API and inspected existing primitives, no implementation proof. Confidence: low.
- **Warning:** A prompt instruction cannot set immutable workspace metadata; a tool filter is not a filesystem fence; a finished worker is not deletion authority; a requested record is not proof of active execution.
- **Known limitations:** No arbitrary preset selection, remote role degradation, cross-repository routing, process sandbox guarantee, atomic storage/start transaction, automatic crash reconciliation, history deletion, or transfer/dirty-tree retirement. Outcome and model history limits reject overflow explicitly.

## Acceptance

1. Freeze child-cwd prerequisite RED before its implementation; freeze Roles RED separately. Implementer changes production code, not handed-off tests. All tests below remain proposed until their owner records execution.
2. Real spawn and fork create/persist assigned child headers and model cwd, preserve parent/siblings, reject invalid explicit cwd, preserve omission, and support valid child cwd without parent cwd. Continuable creation/resume retain the same header.
3. ACP, DSH SDK, Codex, Claude Code, and Antigravity actual adapters pass explicit/configured/parent precedence, invalid/no-fallback, cancellation, and actual process/SDK/wire cwd assertions with owned fixtures.
4. A real Loader Host plus scoped Brain consumer exposes and executes both tools through the model/tool runtime. Correct caller, role persona, actual optional preset, assigned relative-file reads, and logged provenance reach the child/parent model requests.
5. All four canonical roles work; unknown role/Reviewer, missing provider, unsupported persona/filter, invalid filter, and oversized brief reject without silent degradation. Real executor denial covers native/PTC inherited tools; child-local structured capture survives.
6. Queue tests prove exact caller across waits, stale identity rejection, cancellation during allocation/start/result, late returned handle disposal, awaited cleanup, and truthful unproven cleanup. No path retires a worktree.
7. Test Writer leaves a real file and Implementer receives the same assignment/path/file. Role service disposal preserves the work and awaits recording before domain close.
8. Actual durable backend reopen retrieves ordered requested/settled history. Initial write failure prevents dispatch; terminal write failure exposes persistence failure; cancellation is recorded after cleanup; malformed authoritative records reject; unresolved crash residue remains unresolved.
9. Brief/outcome/tool/history limits cover complete UTF-8 output, tiny/exact/multibyte cases, metadata wrappers, non-text output, and explicit overflow without silently clipped history or reports. Provenance never claims parent direct inspection.
10. Record focused unit, Loader, provider, and keyless model-snapshot evidence; update owner docs and source consumers in the implementation change. Transfer without losing unrelated mainline work, rerun affected tests on mainline, then move the completed piece to done and commit. No branch, Reviewer stage, push, or invented passing output.
